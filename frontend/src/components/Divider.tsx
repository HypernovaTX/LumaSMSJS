import { Divider } from '@mui/material';
import { styled } from '@mui/material/styles';

export const LumaDivider = styled(Divider)({
  '&': {
    margin: '4px 0',
    borderColor: 'rgba(0, 0, 0, 0.2)',
    boxShadow: '0 1px 0 rgb(255, 255, 255, 0.2)',
  },
});
